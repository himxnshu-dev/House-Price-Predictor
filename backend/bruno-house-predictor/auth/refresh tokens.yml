info:
  name: refresh tokens
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/api/auth/refresh
  auth:
    type: bearer
    token: "{{refresh_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
