info:
  name: view favorites
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8080/api/favorites
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
