info:
  name: fetch available locations
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:8080/api/predict/locations
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
