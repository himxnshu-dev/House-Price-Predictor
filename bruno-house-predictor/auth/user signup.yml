info:
  name: user signup
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/auth/register
  body:
    type: json
    data: |-
      {
        "name": "user",
        "email": "user@demo.com",
        "password": "user123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
